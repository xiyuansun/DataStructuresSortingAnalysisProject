In order to run report2, simple run the file with the first argument being the text file (make sure to include .txt) that you want read and transformed into an array.
To run report1 adjust the size of the first array in order to change the size of the array being tested, and results will be exported in a .xls file.
